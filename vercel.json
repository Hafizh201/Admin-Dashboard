{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "vite",
  "installCommand": "pnpm install --no-frozen-lockfile",
  "buildCommand": "pnpm --filter @workspace/smart-borrowing build && rm -rf public && mkdir -p public && cp -R artifacts/smart-borrowing/dist/public/. public/",
  "outputDirectory": "public"
}
